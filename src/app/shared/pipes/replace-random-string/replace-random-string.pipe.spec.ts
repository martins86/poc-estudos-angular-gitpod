import { ReplaceRandomStringPipe } from './replace-random-string.pipe';

describe('ReplaceRandomStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceRandomStringPipe();
    expect(pipe).toBeTruthy();
  });
});
